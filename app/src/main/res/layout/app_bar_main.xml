<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.Activities.MainActivity.MainActivity">

    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <include
            android:id="@+id/current_sessions_card"
            layout="@layout/current_sessions_card"/>

        <com.github.clans.fab.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/fab_margin"
            android:elevation="@dimen/fab_elevation"
            android:src="@drawable/ic_add_24dp"
            app:fab_colorNormal="@color/fabNormal"
            app:fab_colorPressed="@color/fabPressed"
            app:fab_hideAnimation="@anim/hide_fab"
            app:fab_shadowColor="@color/shadowBlack"
            app:fab_showAnimation="@anim/show_fab"/>

    </android.support.design.widget.CoordinatorLayout>

</layout>
